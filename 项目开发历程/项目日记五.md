## 今日完成：

- 完成了PromptBuilder提示词构建和ConversationManager会话机管理的构建以及函数的完善
  - 这两个类的所有函数都是第一次见，后续项目MVP发布后需要回来复盘总结
- 进一步完善了Manager的crud方法
  - 也就多写了一个查单行程所有消息的函数

## 感想：

1. 现在，单个crud方法的撰写已经不是问题了，我熟悉了撰写这类函数的基本步骤，不过后续还要多写才行。

2. 接触到了可能是agent独有的类以及一些不那么基础的py语法
   
   - PromptBuilder：包含静态system_prompt的加载（函数内部加载外部prompt）以及用户上下文的拼接。其中拼接部分用上了所谓"*"，即可迭代对象解包（简单说就是把一个列表里面的元素拿出来，跟索引效果一致）
   
   - ConversationState：这个类似乎叫状态机，这样的写法我第一次见（把各个变量以及固定值写进一个类当作集合），目前还没看懂有什么作用
   
   - ConversationManager：这应当是会话管理最重要的类。包含创建会话，追加消息，上下文裁剪，会话状态确认等函数。今天也算是看到熟悉的算法出现了，即滑动窗口。不过说实话，没看懂这个窗口怎么实现的，向窗口追加是有了，但裁剪最早的消息没看出来。

        还行，感觉今天干的事挺多。正常来说，明天就到agent的决策部分了，这是核心，看看明天能不能完成吧。还有，果然还是在完成项目的时候边干边学高效点。

## ***2026.7.15落笔***
